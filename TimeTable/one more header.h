#ifndef one_more_header_h
#define one_more_header_h

#include <string>
#include <cstdio>
#include <vector>
#include "Header.h"

void print(class Tutu * , int);
vector<Tutu> QckSort(vector<Tutu> voyage);
bool comparator(pair<int, int>, pair<int, int>);

#endif
